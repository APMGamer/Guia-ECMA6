// transformar objetos en matrices
const data = {
    frontend: 'Oscar',
    backend: 'Isabel',
    desing: 'Ana',
}

const entries = Object.entries(data);
console.log(entries);

// objects values 

const values = Object.values(data);
console.log(values);